# Instant Space Rust Phase 计划

> 依据：`docs/ARCHITECTURE_RUST.md`  
> 目标：先稳定“地图 + 登录 + 创建 + 我的空间”的闭环，再逐步补齐主理人管理、攻略、实时互动和后台运营。

---


## 产品基线：全球旅行攻略空间

从 2026-07-09 起，后续 Phase 统一围绕 `docs/PRODUCT_VISION.md` 执行：

```text
Instant Space = 全球旅行攻略与共享体验空间平台
Space = 真实旅行地点的数字入口，也是地图默认显示对象
Guide = Space 下的结构化攻略内容，也可作为独立攻略存在
Community = 当前用 Discord/QQ 承接，后续升级站内实时互动
QR/Link = 空间的线下扫码和分享增长入口
```

关键关系：

```text
Space 1 : N Guide
Guide.space_id optional
```

因此，后续开发必须满足：

- 首页第一屏必须先讲清楚产品价值，不能只显示地图；建议主文案：**发现全球旅行攻略空间，扫码进入真实地点的共享体验。**
- 首屏 UX 必须包含：强价值文案、搜索主行动、浏览攻略/创建空间次行动、快速场景入口，并在移动端避免多浮层叠加。
- 首屏必须有图片化视觉和轻量动效：地图点位、扫码手机、二维码、攻略卡片、共享体验提示；不能只有地图、文字和按钮。
- 地图默认显示空间，不默认显示攻略。
- 空间详情页必须展示相关攻略。
- 管理空间时必须能查看、新建、编辑、删除/归档相关攻略。
- 写攻略时必须能选择绑定已有空间，或创建不绑定空间的独立攻略。
- 普通用户只能编辑自己的空间和攻略；admin/super_admin 可管理全部。
- 已保存攻略再次保存必须更新同一条 guide，不能因为保存多次生成重复攻略。

---
## 0. 当前差距判断

对照架构文档，当前项目不是方向错，而是很多能力还停留在“可演示基础版”。下一步不应该继续零散加功能，而要按闭环推进。

### 0.1 已经基本完成

- `/inspace` 首页地图探索。
- MapLibre 本地资源和 `/inspace/ofm` 同源代理。
- 页面切换回首页地图重挂载。
- 中英切换基础能力。
- 统一登录 / 注册。
- 登录后头像入口。
- 创建空间弹窗。
- 弹窗地图选点填写经纬度。
- `/inspace/my-spaces` 基础页面。
- `/inspace/admin` 基础后台入口。
- 私密空间密码版本的领域边界。
- 攻略层级筛选基础能力。

### 0.2 目前最需要改的地方

#### P0：用户闭环不完整

1. 登录后 Header 依赖 `current_session`，但登录成功后全局 session 刷新机制还不够明确。
2. 没有退出登录。
3. 创建空间成功后，首页地图和“我的空间”列表不会自动刷新。
4. Header 当前路由高亮仍偏粗糙，容易一直显示 Explore。
5. 创建空间表单字段太少：缺少空间类型、自定义类型、描述、标签、有效期。

#### P0：我的空间只是列表，不是管理

1. 不能编辑空间信息。
2. 不能关闭 / 重新激活空间。
3. 不能更换空间密码。
4. 不能申请驻留。
5. 不能归档为空间模板。

#### P1：私密空间访问还没形成完整产品体验

1. 访问验证需要更明确地落到 `access_sessions`。
2. 私密空间进入后的页面/聊天权限还不完整。
3. 密码轮换后旧访问不会立刻踢出；但发送消息必须重新校验当前密码版本。

#### P1：攻略系统仍是浏览基础版

1. 没有攻略详情页。
2. 没有结构化攻略编辑器。
3. 用户投稿、个人草稿和后台内容管理流程还不完整。
4. 攻略和空间的关联展示还不完整。

#### P1：后台运营能力不足

1. Admin 只有基础入口和统计。
2. 缺空间管理、攻略管理、驻留审批、用户管理。
3. 缺统一操作结果和错误展示。

#### P2：实时互动未开始

1. 聊天还没有 WebSocket 通道。
2. 求助和游戏还没有产品化页面。
3. 在线人数还不是实时数据。

#### P2：生产工程化还需要补齐

1. 数据导入需要完整迁移旧 SQLite 的空间和攻略。
2. 缺定期任务处理空间过期。
3. 缺统一错误格式、日志追踪和备份脚本。
4. 当前未提交改动较多，需要分阶段整理提交，避免不可追踪。

---

## Phase 1：稳定用户主闭环

### 目标

让普通用户完成完整流程：注册/登录 → 看到头像 → 创建空间 → 地图出现新空间 → 我的空间可见 → 可退出。

### 范围

#### 1. 会话与 Header

- 增加 `logout_user` server function。
- Header 头像菜单包含：我的空间、退出登录。
- 登录 / 注册成功后触发全局 session 刷新或跳转到首页。
- Header 根据当前路径高亮：Explore / Guides / My Spaces。

#### 2. 创建空间弹窗

- 创建成功后：
  - 显示成功状态。
  - 可一键关闭弹窗。
  - 首页地图刷新空间点。
  - 我的空间列表刷新。
- 表单补字段：
  - 空间类型：景点 / 美食 / 公园 / 交通 / 活动 / 自定义。
  - 自定义类型名称。
  - 中文描述、英文描述。
  - 标签。
  - 有效期：默认 24 小时。
- 经纬度字段继续保留，但地图选点是主要交互。

#### 3. 我的空间基础管理

- 我的空间卡片展示：状态、公开性、有效期、坐标、在线数。
- 空间详情抽屉或管理弹窗。
- 先实现只读详情，为 Phase 2 编辑做准备。

### 验收标准

- 未登录点击创建空间，提示登录。
- 登录后 Header 显示头像和用户入口。
- 点击退出后 Header 回到 Sign in。
- 创建空间后，不刷新页面也能在首页地图看到新 marker。
- `/inspace/my-spaces` 能看到刚创建的空间。
- 移动端弹窗不横向溢出。

### 必须测试

```bash
npm run test:browser
npm run test:map-runtime
DATABASE_URL=postgres://instant_space:instant_space_pass@127.0.0.1:5432/instant_space_rust cargo test --workspace
```

---

## Phase 2：主理人空间管理

### 目标

让主理人能管理自己的空间生命周期和私密访问。

### 范围

#### 1. DB / repository

- 增加更新空间基础信息 repository。
- 增加空间生命周期操作 repository：
  - close
  - reactivate
  - regenerate_password
  - archive_template
  - apply_resident
- 密码轮换必须 `password_version + 1`。

#### 2. Server Functions

- `update_my_space`
- `close_my_space`
- `reactivate_my_space`
- `regenerate_space_password`
- `archive_my_space_template`
- `apply_space_resident`

权限规则：

```text
current_user.id == spaces.host_user_id OR current_user.role is admin
```

#### 3. UI

- 我的空间详情编辑表单。
- 关闭 / 重开按钮。
- 更换密码按钮，并显示新的 WiFi 热点名格式。
- 申请驻留按钮。
- 归档模板入口。

### 验收标准

- 用户只能修改自己的空间。
- 非 owner 请求被拒绝。
- 密码轮换后旧 access session 仍可保持页面读取；发送消息时必须提示重新输入密码。
- 关闭空间后首页不再作为 active 空间展示，或按产品规则显示为 closed。
- 重新激活后回到地图。

### 必须测试

- Rust repository 单元/集成测试。
- Browser：我的空间编辑和生命周期按钮。
- 私密访问版本失效测试。

### Phase 2 当前实现状态（2026-07-05）

已完成主理人最小管理闭环：

- 我的空间卡片展开管理面板。
- 编辑基础信息：名称、位置、坐标、公开性。
- 关闭空间 / 重新激活。
- 重置空间密码，并显示新 6 位密码和 `InstantSpace_XXXXXX` 热点名。
- 申请驻留。
- 归档为空间模板状态。
- 服务端校验 owner 或 admin 权限。
- 密码重置时 `password_version + 1`，旧访问会话不立刻踢出；发送消息时若版本不匹配会提示重新输入密码。

仍留到后续优化：

- 更完整的空间详情页。
- 编辑描述、标签、自定义类型、有效期的二次管理。
- 操作确认弹窗和审计日志。
- Admin 端统一审批和管理界面。

### Phase 2.1 创建空间定位体验（2026-07-05）

围绕“创建空间弹窗”完成一次稳定性补强：

- 创建空间不再要求用户手动填写空间密码。
- 服务端创建时自动生成 6 位密码，并按 `InstantSpace_XXXXXX` 返回热点名。
- 创建成功后密码只在当前结果区展示一次，后续需要主理人在“我的空间”里重置。
- 创建弹窗地图加入 MapLibre 放大 / 缩小控件。
- 地图选点后会自动填写经纬度，并尽力反向匹配国家、省/州、城市、区县字段。
- spaces 表增加 `country` 字段，列表、创建、编辑和我的空间均同步该字段。

说明：国家 / 省州 / 城市 / 区县的完整层级库后续应做成服务端地理数据表；当前阶段优先保证地图选点自动匹配，人工字段仍保留可编辑。

### Phase 2.2 服务端地理库与详细位置（2026-07-05）

已继续补齐创建空间的位置能力：

- 新增服务端 `geo_places` 地理表，当前线上已导入 GeoNames `cities5000` 级别全球地点数据约 6.9 万条。
- 新增地理查询接口：
  - 国家列表。
  - 国家下省 / 州 / 大区。
  - 国家 + 省州下城市 / 镇。
  - 区县 / County 选项。
  - 经纬度服务端反向匹配最近地点。
- 创建空间位置层级改为服务端 datalist，可输入也可从候选中选。
- 空间新增更细字段：
  - 具体地点 / 景点。
  - 详细地址 / 备注。
- 创建表单为主要空白输入框增加 placeholder，提示用户应该填写什么。
- 创建地图修正加载遮罩阻挡交互的问题，现在可拖动移动视野，也保留缩放和点击选点。

后续如果要做到“所有国家完整行政区划到区县/街道级”，需要再接入更细粒度数据源（如各国 OSM/Nominatim 或商业 geocoder）。当前阶段先保证全球城市级和主要行政区可用。

### Phase 2.3 我的空间管理弹窗（2026-07-05）

针对主理人管理体验继续优化：

- “管理空间”从卡片内展开改为独立弹窗，避免把列表撑乱。
- 管理弹窗内加入地图选点，可重新选择空间位置。
- 点击地图会同步经纬度，并尽力自动更新国家、省州、城市、区县、具体地点。
- 管理弹窗增加空间密码说明：
  - 旧密码已加密保存，不能反查显示。
  - 主理人可点击“重置并显示新密码”生成新密码。
  - 新密码和 `InstantSpace_XXXXXX` 热点名只展示一次。
- 浏览器测试覆盖：管理弹窗打开、地图选点、重置密码、空间生命周期操作。

### Phase 2.4 删除空间与移动端设备识别（2026-07-05）

继续补齐主理人和安卓移动端体验：

- 我的空间管理弹窗新增“删除空间”。
- 删除采用软删除：空间状态置为 `archived`，不再出现在首页和“我的空间”列表，后续后台仍可做审计/恢复能力。
- 删除需要二次点击“确认删除”，避免误操作。
- `/my-spaces` 默认不再展示 `archived` 空间。
- 增加前端设备识别：根据 User-Agent 和视口宽度设置 `html[data-device="mobile|desktop"]`。
- 安卓/移动端紧凑布局优化：
  - 顶部导航更紧凑。
  - 探索筛选面板限制高度并允许内部滚动。
  - 地图控制和底部空间抽屉减少遮挡。
  - 创建/管理弹窗在手机上接近全屏，避免内容塞满并横向溢出。

---

## Phase 3：私密空间与聊天入口

### 目标

把“私密空间需要密码”从展示功能升级成完整访问系统，并为实时聊天打基础。

### 范围

- `verify_space_password` 写入 `access_sessions`。
- 私密空间详情中显示进入空间入口。
- 进入私密空间前检查 access session。
- Chat 页面或抽屉先做非 WebSocket 的持久化消息列表。
- 发消息时校验 password version。

### 验收标准

- 未验证不能进入私密聊天。
- 验证后可进入。
- 空间密码轮换后旧验证不能发消息。
- 刷新页面后短期内仍保留访问权限。

### Phase 3 当前实现状态（2026-07-05）

已完成私密空间和聊天入口的非实时闭环：

- `verify_space_password` 会写入 `access_sessions`，并下发 12 小时 HttpOnly 访问 cookie。
- 私密空间验证成功后显示“进入私密聊天”入口。
- 新增 `/inspace/spaces/:space_id` 空间聊天页。
- 进入聊天页前会检查：
  - 公共空间可直接进入。
  - 私密空间必须有有效 access session。
  - access session 存在且未过期即可继续查看页面，避免主理人改密后把已在空间内的人立刻踢出。
- 新增持久化聊天消息：
  - 列表读取数据库 `chat_messages`。
  - 发送消息写入数据库。
  - 发消息时再次校验私密访问和当前密码版本；版本不一致提示重新输入密码。
- 浏览器刷新后，12 小时内仍可进入已验证私密空间。
- 增加测试覆盖：
  - 私密空间验证 → 进入聊天 → 发送消息 → 刷新仍可访问。
  - 密码轮换后旧 access session 仍可查看页面，但不能继续发送消息。

后续 Phase 6 再把当前持久化聊天升级为 WebSocket 实时聊天、在线人数和断线重连。

### Phase 3.1 社群密码分发与空间详情提示（2026-07-05）

新增产品规则：

- Discord/QQ 总频道名为“即时空间”。
- 总频道固定结构：
  - `#全局公告`
  - `#空间列表`
  - 按省份组织板块，例如“广东省 / 北京市 / 四川省”。
  - 每个空间在省份板块下创建讨论组，例如“汕头中山公园”“广州白云山”。
- 权限逻辑：
  - 远程用户可进入总频道看公告和空间列表。
  - 具体空间讨论组需要申请加入。
  - 主理人审核通过后，成员才能看到实时密码和更新。
- 热点名固定格式：`InstantSpace_123456`，即 `InstantSpace_` + 6 位数字密码。
- 创建空间成功后向主理人提示：
  - 设置手机热点名。
  - 去 Discord/QQ 对应省份板块创建讨论组。
  - 向频道管理员申请讨论组管理员身份。
  - 只在讨论组里向审核通过的成员发布密码。
- 空间详情页固定展示社区入口：
  - Discord：`https://discord.gg/zsmYWvXyy`
  - QQ 频道：`https://pd.qq.com/s/8ru51ih0m?b=9`
  - 文案：在社群内搜索空间名获取进入密码。

---

## Phase 4：攻略系统升级

### 目标

让攻略成为可浏览、可编辑、可关联空间的正式内容系统。

### 范围

- 攻略详情页。
- 结构化 sections 展示。
- 用户创建攻略草稿。
- 后台内容管理：管理员可管理、发布、归档攻略，但不作为逐条人工审核员。
- 攻略关联空间。
- 图片 URL 管理，后续再考虑上传。

### 验收标准

- 省 → 市 → 区 → 景点筛选稳定。
- 攻略详情 URL 可分享。
- 用户投稿默认 draft。
- 管理后台可发布 / 归档 / 管理攻略。

### Phase 4.1 攻略详情页与分享 URL（2026-07-06）

已完成攻略详情的第一阶段：

- 新增可分享攻略详情路由：
  - `/guides/:guide_id`
  - `/inspace/guides/:guide_id`
- 攻略列表项可点击进入详情页。
- 新增 `get_guide_detail` server function，只返回 `published` 攻略。
- 攻略详情展示：
  - 中英标题。
  - 摘要 / 正文。
  - 省市区景点位置。
  - 攻略类型、分类、作者。
  - cover image。
  - 结构化 `sections`。
  - 图片 URL 列表。
  - 关联空间入口。
- 未发布或不存在的攻略显示空状态，并可返回攻略列表。
- 浏览器测试覆盖攻略列表 → 详情页 → sections 展示。

后续 Phase 4.2 继续做结构化攻略编辑器和草稿投稿。

### Phase 4.2 结构化攻略编辑器草稿版（2026-07-06）

已完成结构化编辑器的结构化对象版闭环：

- 新增投稿入口：
  - `/guides/new`
  - `/inspace/guides/new`
- 攻略列表页增加“提交攻略 / Submit guide”入口。
- 未登录用户访问编辑器会提示登录。
- 登录用户可以打开结构化攻略编辑器。
- 顶层用一个 `GuideDraft` signal 持有整篇攻略对象，不再把每个字段拆成独立 signal。
- 组件拆分为：
  - `GuideEditor`：基本信息、摘要、板块列表、封面图片、底部保存按钮。
  - `SectionEditor`：受控编辑单个结构化板块。
  - `ImageManager`：受控管理 URL 图片数组，使用 `Url::parse()` 校验并展示预览网格。
- 编辑器支持 Guide 字段：
  - 中英文标题。
  - 中英文摘要。
  - guideType、category。
  - 省 / 州、城市、区县、地点。
  - images 图片 URL 数组（第一张作为封面）。
  - sections 结构化板块数组。
  - status：draft / published / archived。
  - featured：仅 admin mode 显示入口。
- Section 字段使用新结构：
  - `id`
  - `type`
  - `title_zh/title_en`
  - `content_zh/content_en`
  - `images`
- 后端 `guides.sections` 和 `guides.images` 继续使用 jsonb，写入时序列化 Rust struct，读取时兼容旧 seed 的 `heading/body` 格式。
- `create_guide_draft` server function 支持保存草稿、保存并发布、取消发布（archived）。
- 投稿默认仍是用户内容流；admin 是管理后台，不定义为逐条审核员。
- repository 测试覆盖结构化 sections 入库和清理。

后续 Phase 4.3 / 4.4 继续做“我的攻略草稿列表”和 admin 后台内容管理（发布 / 归档 / 编辑）。后期可加入 AI 持续辅助 admin 管理空间和攻略。

### Phase 4.3 空间与攻略关系修正（当前优先级）

目标：解决“我有多个空间时，写攻略到底对应哪个空间；如果不想绑定空间怎么办”的产品逻辑问题。

范围：

- 攻略编辑器增加明确的空间绑定选择：
  - 绑定已有空间。
  - 不绑定空间，作为独立攻略。
- 从某个 Space 详情或管理入口点击“写攻略”时，自动带入当前 `space_id`。
- Space 详情页展示该空间下的攻略。
- 管理 Space 弹窗/页面增加“相关攻略”模块：
  - 写新攻略。
  - 查看已绑定攻略。
  - 编辑攻略。
  - 删除/归档攻略。
- 管理端或 super_admin 视角能看到并管理所有空间和攻略。
- 普通用户只能编辑/删除自己创建的攻略，不能编辑别人创建的空间或攻略。
- 保存已存在攻略必须更新同一条记录，不能重复生成多份攻略。

验收标准：

- 用户管理一个空间时，可以直接看到该空间关联的攻略。
- 用户从该空间写攻略，保存后攻略自动绑定该空间。
- 用户从攻略页新建时，可以选择任一可用空间，也可以选择独立攻略。
- 普通用户看不到别人内容的编辑/删除入口；直接调用服务端也会被拒绝。
- admin/super_admin 可以管理全部空间和攻略。


### Phase 4.3 落地进度（2026-07-11）

已完成：

- 管理空间弹窗增加「相关攻略」模块：写新攻略 / 查看 / 编辑 / 归档删除。
- 主理人可在管理入口看到该空间下 draft + published + archived 攻略。
- 地图空间详情抽屉展示已发布相关攻略，并提供「写攻略」入口（带 `space_id`）。
- 服务端 `list_manageable_space_guides`：仅 space owner 或 admin 可看未发布攻略。
- 权限：普通用户只能编辑自己的攻略；admin 可管理全部；保存仍走 update/create 更新同一条记录。

待 Phase 4.4：

- 空间分享链接与二维码。

### Phase 4.4 分享链接与二维码入口

目标：让 Space 成为线下扫码和朋友分享的落地页。

范围：

- 每个 Space 生成稳定分享链接。
- 每个 Space 可生成二维码。
- Space 详情页增加分享区。
- 管理 Space 时可复制链接、下载/查看二维码。
- 后续为餐馆/景点二维码物料和 3D 场景入口预留字段。

验收标准：

- 扫描二维码后进入 Space 详情页。
- Space 详情页可看到基础信息、相关攻略、社群入口和进入空间入口。
- 链接不直接落到单篇攻略，除非用户明确分享的是攻略详情页。

---


### Phase 4.4 落地进度（2026-07-11）

已完成：

- 每个 Space 有稳定分享路径：`/inspace/spaces/{id}`。
- 地图空间详情抽屉、空间页、管理空间弹窗均增加分享区。
- 支持一键复制完整分享链接。
- 支持显示二维码（扫码进入空间详情页，不直接落到单篇攻略）。
- 管理空间时可复制链接、查看二维码，便于线下物料。

后续可选：

- 本地生成二维码图片下载（PNG）。
- 为餐馆/景点物料预留 `qr_asset_url` 字段。

## Phase 5：后台运营

### 目标

让 admin 成为真实运营后台，而不是只有统计页。admin 负责管理空间和攻略，不作为逐条人工审核员；后期可加入 AI 助手持续辅助发现异常、整理内容和提出运营建议。

### 范围

- Admin 空间列表、搜索、状态筛选。
- Admin 空间编辑和生命周期操作。
- Admin 攻略列表、编辑、发布、归档。
- 驻留申请审批。
- 用户列表和角色管理。
- 操作日志。

### 验收标准

- 普通用户无法访问 `/inspace/admin` 数据。
- admin/super_admin 权限分明。
- 所有危险操作有确认。
- 后台操作可审计。

### Phase 5 落地进度（2026-07-12）

已完成 admin 后台三大管理块 + 审计：

- **空间管理** `/inspace/admin/spaces`：
  - `list_all_spaces_admin` 列全站空间（含 archived/已删除，供审计恢复）。
  - 客户端搜索 + 状态筛选。
  - 复用现有生命周期 server fn（关闭 / 重新激活 / 归档模板 / 删除），admin 绕过 owner 校验。
  - 删除需二次确认。
- **攻略管理** `/inspace/admin/guides`：
  - `list_all_guides_admin` 列全状态攻略（draft / published / archived）。
  - 状态筛选 + 一键发布 / 归档（`set_guide_status_admin`）。
  - 编辑复用现有 `/inspace/admin/guides/:id/edit` 编辑器。
- **驻留审批** `/inspace/admin/resident-applications`：
  - `list_resident_applications` 列待审申请（带空间名 + 申请人邮箱）。
  - 批准（可填驻留天数，写 `expires_at`）/ 拒绝。
- **用户角色** `/inspace/admin/users`：
  - `list_users` 列全部用户 + 角色。
  - 改角色仅限 super_admin（防 admin 互相提权）；禁止 super_admin 自我降级。
- **操作审计**：
  - 新增 `admin_audit_log` 表（append-only）。
  - 敏感操作（批准 / 拒绝驻留、改角色）写审计记录。
  - Dashboard 展示最近 200 条审计日志。
- 所有 admin server fn 走 `require_admin_user()` 门；改角色额外要求 super_admin。

后续可选：

- 空间管理弹窗接入地图重新选点（当前 admin 空间页是轻量列表，重定位仍走 `/my-spaces` 管理弹窗）。
- 审计日志分页 / 筛选 / 导出。
- admin 空间编辑（描述、标签、有效期）的完整表单。

---

## Phase 6：实时互动

### 目标

实现空间内实时体验：聊天、求助、小游戏、在线人数。

### Phase 6.1 落地进度（2026-07-20）

已完成实时聊天第一阶段：

- 新增 Axum WebSocket 房间路由：`/ws/spaces/:space_id`，线上通过 nginx 暴露为 `/inspace/ws/spaces/{id}`。
- 同一空间使用 Tokio broadcast 广播消息，多个浏览器会实时收到同一条消息。
- 连接建立后发送最近 100 条历史消息，数据库仍是消息持久化来源。
- `active` 公共空间允许访客连接；私密空间必须带已有 `instant_access_{space_id}` 访问会话。
- 私密空间每次发送消息都会重新检查密码版本；主理人更换密码后，旧连接会收到重新验证提示并断开。
- 在线人数按 WebSocket 房间实时维护，并同步回 `spaces.online_count`；服务重启时先清零残留计数。
- 前端新增实时状态、在线人数、断线指数退避重连和双语错误提示。
- 保留原有 server function 发送接口作为无 WebSocket/旧客户端的回退路径，并同步广播到在线房间。
- 历史消息查询限制为最近 100 条，避免空间消息无限增长导致首屏越来越慢。
- 已通过公网双客户端验证：两个客户端同时进入同一公共空间，一个发送消息，两个客户端均收到广播，连接关闭后在线人数恢复为 0。

当前仍留到 Phase 6.2：

- 求助消息类型和通知状态。
- WebSocket 房间级限流、消息频率限制和连接上限。
- 断线后未发送消息的离线队列/送达状态。
- 在线成员列表和主持人/成员角色展示。

### 范围

- Axum WebSocket 路由。
- Tokio broadcast 或 channel 管理房间。
- 在线人数统计。
- 聊天消息持久化。
- 求助消息。
- 游戏状态同步。

### 验收标准

- 同一空间多个浏览器实时收到消息。
- 私密空间 WebSocket 握手校验 access session。
- 断线重连后能读取历史消息。

---

## Phase 7：数据迁移与生产化

### 目标

把旧数据完整迁移进 Rust 版，并让服务具备长期运行能力。

### 范围

- 完整导入旧 SQLite 的 spaces、guides、users。
- 导入后数量和关键字段校验。
- 空间过期定时任务。
- 数据库备份脚本。
- 日志、错误格式、健康检查。
- CI 或部署前脚本固定测试流程。

### 验收标准

- 旧项目代表数据全部可在 Rust 版查询。
- 导入脚本可重复执行或有清晰幂等策略。
- 部署前测试一键执行。
- 出错有日志可查，不靠猜。

---

## 推荐执行顺序

当前最应该先做：

1. **产品基线修正**：按 `PRODUCT_VISION.md` 固定 Space / Guide / QR / Community 的关系。
2. **Phase 4.3**：修正空间与攻略关系，尤其是管理空间里的相关攻略模块。
3. **Phase 4.4**：补空间分享链接和二维码入口。
4. **Phase 5**：完善 admin 后台对空间和攻略的全局管理。
5. **Phase 6**：再升级实时互动和未来 3D 共享体验。

原因：当前基础登录、创建空间、空间管理、私密访问、攻略编辑器已经具备雏形；下一步最重要的是让用户清楚理解“地图上显示空间，攻略挂在空间下，也可以独立存在”。如果这个关系不稳，继续做实时聊天或 3D 都会产生更多返工。

---

## Phase 1 细化任务清单

建议下一轮开发只做以下内容，不混入 Phase 2：

- [ ] 增加退出登录 server function。
- [ ] Header 头像改为菜单。
- [ ] 登录成功后刷新 Header 状态。
- [ ] Header 路由高亮修正。
- [ ] 创建空间成功后刷新地图和我的空间。
- [ ] 创建空间表单增加类型、描述、标签、有效期。
- [ ] 我的空间卡片补状态和有效期显示。
- [ ] 增加/更新浏览器测试。

Phase 1 完成后，再开始空间编辑和密码轮换。

---

## Phase 8：数字生命与云上家（已建立基础）

当前已经完成数字生命实验田的第一层：每个用户拥有一个一对一绑定私密 Space 的云上家；活着的宠物可在主人在线时「在侧」、离线时「在家」，主人完成真实地点在场验证后会为同行宠物记录足迹；离世后才可蒸馏为小传、铭文、生命地图与纪念空间。

Phase 8 的后续体验不再单独扩张为孤立页面，而是并入 Phase 9 的 Space / Scene / Presence 世界规则。

## Phase 9：空间世界化基础层

### 目标

让 Space 从传统详情页面升级为可以进入、活动、经营、沉淀历史并直接抵达的数字世界，同时保留现有网页、地图、Guide、SEO 与 Agent API。

### 核心模型

```text
Space     长期存在的数字地点、领地或世界节点
Scene     进入 Space 后看到和活动的可视化环境
Object    把内容和功能转译为建筑、人物与物件
Portal    Scene 内通往其他 Space 的探索式入口
Teleport  地图、搜索、QR、链接等直接抵达目标 Scene/出生点
Host      Space 的长期照料者、建设者与叙事者
Presence  用户及数字生命当前所在的 Space / Scene / 位置
```

空间层级用于组织世界，但不是强制访问路径。用户既可以沿世界大厅逐级探索，也必须能从地图、搜索、二维码、NFC、Wi-Fi、链接、AI、好友邀请、胶囊和访问历史直接传送到目标 Space。

### Phase 9.1：领域模型与兼容数据库

- `spaces.world_role`：hub / place / micro / home / memorial；不替代用户已有自定义类型。
- `scenes`：一个 Space 可有多个场景。
- `scene_objects`：游客中心、AI 导游、留言墙、公告牌、主理人、Portal、胶囊和展示物。
- `scene_spawn_points`：入口、门外、Portal 落点等出生点。
- `space_relations`：父子、相关、Portal、家庭与纪念关系。
- `space_host_tenures`：主理人任期与继任历史。
- `world_presences`：用户及同行数字生命的当前位置。
- `space_entry_events`：进入方式、来源和验证状态的不可变记录。

现有云上家作为第一批 home Scene 自动接入；其他历史 Space 在首次使用时按需建立默认 Scene，避免一次性重写两千多个空间。

### Phase 9.2：统一进入与直接传送

统一进入意图：

```text
目标 Space + 目标 Scene + 出生点 + 进入方式
+ 来源 Space/Object + 验证状态
```

进入私密或现场空间时先验证；验证失败停留门外，不读取受保护内容。登录用户进入后更新 Presence，活着的同行宠物同步传送。

### Phase 9.3：长期主理人治理

区分创建者、当前主理人、共同主理人、历史主理人、居民、访客、系统托管与待认领状态。主理人可维护内容、布置 Scene、放置 Object、设置 Portal 和出生点；主理人更换后，空间身份、历史和场景不得消失。

**落地进度（2026-08-04）：已完成首个产品闭环。**

- 新增 `spaces.host_governance_state`：`hosted / recruiting / system_care`，并保留公开招募说明。
- 新增不可变 `space_governance_events`，记录共同主理、系统看护、交接、退出与重新招募。
- `space_host_tenures` 成为 Scene/Object/Portal/出生点管理权限的正式来源；旧 `space_members.role='host'` 不再单独授予世界编辑权。
- 用户工作台已支持：邀请共同主理人、管理员委任系统看护、结束协作任期、主理权交接、主动退出、招募说明、历史任期与治理记录。
- 空间详情已展示当前主理人、共同主理人与历任主理；交接后 Space、Scene、故事与历史不删除。
- 主理人或共同主理人均能在“我的空间”看到可维护 Space；只有 primary 或 admin 能改变治理关系。
- 管理员批准认领时同步恢复 `hosted` 状态、写入 primary 任期和治理事件。
- 生产迁移 `20260804000200_host_governance.sql` 已部署；浏览器用两名临时用户真实验证 `共同主理 → 交接 → 历史 → 退出 → 重新招募`，测试数据已清理。

仍待 Phase 9.7 补齐：Scene/Object/Portal/出生点的可视化编辑器、草稿发布、回滚和对象级操作审计。

### Phase 9.4：最小 Scene Runtime

先实现轻量 2D/卡通化大厅：点击移动、靠近或点击物件触发、NPC 对话、Portal 传送、手机点击、键盘、减少动效和低性能模式。不做战斗、装备、复杂经济和大规模实时多人世界。

### Phase 9.5：内容对象化

首批把现有数据映射为游客中心、AI 导游、留言墙、公告牌、主理人角色与 Portal。底层 Guide、故事、聊天和 API 数据继续保留，Scene 成为普通用户新的主要体验入口。

**第一批闭环已上线（2026-08-04），本阶段尚未全部完成。**

- 默认 Scene 改为幂等补齐：历史 Scene 再次进入时，会补上缺失对象并重新绑定当前真实内容，不因已有一个对象而跳过整批升级。
- 城市大厅首批对象：游客中心、问路人、城中旧闻、今日城事；具体地点首批对象：空间志、主理人、留言墙、今日所见、埋信处。
- 对象不复制业务正文，只保存 `content_kind/content_id`，继续读取现有 Guide、故事、讨论、主理人与胶囊数据。
- “问路人”目前只读取并推荐现有空间志；生成式 AI 导游尚未接入，不把静态推荐伪装成 AI 对话。
- Portal 生成器只读取已确认的 `space_relations(child/portal)`，不根据名称或距离猜测层级。当前生产关系表为空，因此云阳县城大厅尚无真实 Portal。
- 首页热门空间/故事已接登录 Gate：未登录者在当前页登录，成功后自动续接原 Space；私密空间继续进入原有口令验证。
- 云阳县城大厅已绑定一条真实公开故事；外滩具体地点已幂等补齐“埋信处”，并绑定真实未开启胶囊。

仍待完成：为经过核验的城市与地点建立真实空间关系、完整 AI 导游、主理人 Scene/Object/Portal 编辑器，以及对象级发布与回滚。

### Phase 9.6：三个样板

1. 云上家：用户、宠物、庭院和家门验证；
2. 城市大厅：地标、游客中心、AI 导游和下级 Portal；
3. 具体地点：主理人、空间志、留言墙、现场验证和胶囊。

**具体地点样板第一批闭环（2026-08-04），Phase 9.6 尚未全部完成。**

- 外滩 Scene 的“埋信处”已成为可接近、可触发的世界对象；用户从对象 Bottom Sheet 打开胶囊时，不跳离世界页，而是在当前页的 Space Modal 中直接进入独立 Capsules 面板。
- 胶囊继续复用既有现场验证和密码安全链路，不新增第二套门禁：埋信需要空间 Wi-Fi 口令与本机定位；启封需要指定收信人到场、通过空间现场验证、输入胶囊主人私下告知的胶囊口令，并满足开放时间。
- Capsules 面板以“人在此地 / 空间口令 / 胶囊私语”三步呈现。故事页不再内嵌完整胶囊架，仅保留一个轻量入口，避免内容重复和卡片嵌套。
- 1440×900、768×1024、390×844 三种视口已完成真实浏览器回归：世界对象到胶囊面板全程 URL 不跳转，无横向溢出、控制台错误或失败请求；证据见 `output/playwright/phase96-place-capsules/`。

仍待完成：云上家与城市大厅样板的完整产品化、具体地点中更完整的主理人和空间历史系统、生成式 AI 导游、经核验的真实 Portal 数据，以及 Scene/Object/Portal/出生点编辑和发布治理能力。

### Phase 9.7：主理人场景编辑器

提供模板选择、对象放置、内容绑定、Portal 目标、出生点、桌面/手机预览、草稿发布、版本回滚和操作审计。首版采用网格化模板，不做无限自由的游戏编辑器。

### 验收标准

- 同一 Space 可通过 Portal 和直接链接进入；
- 人物出现在指定 Scene 和出生点；
- 用户及同行宠物 Presence 同步切换；
- 私密/现场 Space 先验证后进入；
- 主理人可编辑并发布 Scene；
- Scene 至少有游客中心、AI 导游、留言墙和 Portal；
- Space 更换主理人后历史与 Scene 不丢失；
- 原网页、地图、Guide、SEO 和 Agent API 保持可访问；
- 手机、键盘、reduced-motion 与轻量模式均可用。

### Phase 9.4A — 可行走云上家原型（2026-08-04 已上线）

- [x] 删除旧 CSS 庭院与右栏说明式交互
- [x] Phaser 仅在世界路由按需加载，不改写现有 Leptos 网站壳
- [x] Tiled Object JSON 作为云上家场景布局起点
- [x] 点击/键盘移动、宠物跟随、距离触发、单一情境动作
- [x] 内容 Bottom Sheet、Portal 链接与关闭后回到原位置
- [x] SSR 文本回退、低性能与 reduced-motion 基线
- [x] 手机、iPad 横屏、桌面生产浏览器验收
- [ ] 真实障碍碰撞与路径规划
- [ ] Axum WebSocket Presence 位置广播
- [ ] 主理人可视化场景布置与 Tiled 导入契约

### Phase 9.4B — Space 弹窗工作区与 Scene 内容桥（2026-08-04 已上线）

- [x] 全局 Space Modal：当前页面原地打开，保留直接 URL 回退
- [x] 简介 / 主理人 / 故事 / 空间志 / 分享 / 讨论同窗切换
- [x] 空间志在工作区内阅读，不再跳详情页
- [x] 私密验证、实时聊天、求助、刻字继续复用原业务逻辑
- [x] Phaser Object：普通内容 / 故事 / 今日现场映射到 Space Modal 对应面板
- [x] Portal 与真正 Scene 切换保持导航语义
- [x] 桌面弹窗、手机全屏 Sheet、Esc、焦点循环/归还、reduced-motion
